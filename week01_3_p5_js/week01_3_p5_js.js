
function setup() {
    initializeFields();
    initializeFields();
    createCanvas(500, 500);
}

function draw() {
    background(color(0x60, 0xD8, 0xD5));
    fill(color(0xEA, 0x0C, 0x0C));
    rect(100, 100, 150, 100);
    rect(mouseX, mouseY, 150, 100);
}

function initializeFields() {
}

function initializeFields() {
}
